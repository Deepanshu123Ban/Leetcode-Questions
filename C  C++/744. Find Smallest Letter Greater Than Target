class Solution {
public:
    char nextGreatestLetter(vector<char>& letters, char target) {
        for(int i=0;i<letters.size();i++)
        {
            int a=letters[i];
            if(target<a)
            {
                return letters[i];
            }
        }
        return letters[0];
    }
};
